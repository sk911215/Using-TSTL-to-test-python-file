# Using-TSTL-to-test-python-file

First refer to tstl repository to install tstl.

Next copy python SUT linklist.py file and test tstl file to <tstl-root>/generators.

Then type:
python <tstl-root>/generators/randomtester.py 
to run and test SUT

